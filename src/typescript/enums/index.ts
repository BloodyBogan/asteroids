export enum EStates {
    MainMenu = 'MainMenu',
    HighScores = 'HighScores',
    Settings = 'Settings',
    Help = 'Help',
    Play = 'Play',
    Pause = 'Pause',
    EndGame = 'EndGame',
}

export enum EKeyNames {
    UP = 'UP',
    RIGHT = 'RIGHT',
    LEFT = 'LEFT',
    SPACE = 'SPACE',
    P = 'P',
    TAB = 'TAB',
    ENTER = 'ENTER',
}

export enum EImageNames {}

export enum EAudioNames {
    LASER = 'LASER',
    ASTEROID = 'ASTEROID',
    BOOST = 'BOOST',
    DEATH = 'DEATH',
}
